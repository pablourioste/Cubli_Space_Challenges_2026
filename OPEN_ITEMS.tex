% ============================================================================
%  OPEN ITEMS REGISTER -- Cubli Technical Design Document
% ----------------------------------------------------------------------------
%  Standalone working document. NOT input by main.tex -- it is a checklist for
%  the team and a briefing sheet to hand to an assistant, not part of the TDD.
%
%  Compile on its own:  pdflatex OPEN_ITEMS.tex
%
%  Scope: every \textcolor{red}{TBD} in sections/*.tex, every empty section,
%  every comment-only stub and every missing figure, as of the scan date below.
%  Line numbers are those of the files at scan time and will drift as the
%  document is edited; the table/label names are the stable identifiers.
%
%  Counts at original scan: 97 TBD markers, 4 empty sections, 20 comment-only
%  stubs, 21 missing figures.
%
%  UPDATE (sizing closure): A1 (6), A2 (4) and A8 (8) are populated from the
%  converged run of analysis/SIZING.py, and A5's W3 quantity (1) is committed
%  -- 19 TBD closed, leaving 78. The three script-versus-document conflicts
%  recorded in Section 1 are resolved. A4's six TBDs are untouched: they need
%  the packaging study, which remains the head of the chain.
% ============================================================================
\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[a4paper,margin=2.2cm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{ragged2e}
\usepackage{xcolor}
\usepackage{siunitx}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{parskip}

\hypersetup{colorlinks=true, linkcolor=blue, urlcolor=blue,
            pdftitle={Cubli TDD - Open Items Register}}

\DeclareSIUnit{\rpm}{rpm}
\newcolumntype{L}[1]{>{\RaggedRight\arraybackslash}p{#1}}
\renewcommand{\arraystretch}{1.15}
\captionsetup{font=small, labelfont=bf, skip=6pt}

% Status shorthands, so the priority language is consistent across the tables.
\definecolor{blockcol}{HTML}{C00000}
\definecolor{readycol}{HTML}{70AD47}
\definecolor{waitcol}{HTML}{BF8F00}
\definecolor{closedcol}{HTML}{2E75B6}
\newcommand{\blocker}{\textcolor{blockcol}{\textbf{BLOCKER}}}
\newcommand{\ready}{\textcolor{readycol}{\textbf{READY}}}
\newcommand{\waiting}{\textcolor{waitcol}{\textbf{WAITING}}}
\newcommand{\closed}{\textcolor{closedcol}{\textbf{CLOSED}}}

\pagestyle{fancy}
\fancyhf{}
\setlength{\headheight}{14pt}
\fancyhead[L]{\textbf{Open Items Register}}
\fancyhead[R]{Cubli TDD}
\fancyfoot[R]{Page \thepage\ of \pageref{LastPage}}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.4pt}

\title{\textbf{Open Items Register}\\[0.4em]\large Technical Design Document --- Cubli}
\author{Armonia --- Space Challenges 2026}
\date{Scan of \today}

\begin{document}
\maketitle
\thispagestyle{fancy}

\section*{How to use this document}
\addcontentsline{toc}{section}{How to use this document}

This register lists every unresolved item in the TDD: the \textcolor{red}{TBD}
markers, the sections that are empty, the subsections that exist only as
comments, and the figures that are referenced but not drawn. It is a working
checklist, not part of the submitted document.

Three status labels are used throughout:

\begin{itemize}
  \item \ready{} --- decidable now. Nothing upstream is missing; the item is
        waiting on a decision or a datasheet lookup, not on data.
  \item \waiting{} --- blocked on an upstream item, named in the row.
  \item \blocker{} --- blocks a large number of downstream items. Close these
        first.
  \item \closed{} --- resolved since the original scan. Retained in the register
        so the closure is auditable rather than silently deleted.
\end{itemize}

Section~\ref{sec:chain} states the dependency chain, which is what makes the
ordering non-arbitrary: roughly a quarter of the register closes automatically
once the packaging study closes.

\section{Dependency chain}
\label{sec:chain}

Most items are not independent. The governing chain is

\begin{center}
$V_{\min} \;\rightarrow\; L \;\rightarrow\; M \;\rightarrow\; h_w \;\rightarrow\;
I_{w,\text{target}} \;\rightarrow\;$ wheel design, feasibility gate, test thresholds
\end{center}

so the packaging study (item \textbf{A4}) is the true head of the register. The
parametric calculator \texttt{analysis/SIZING.py} implements Stages~1--6 of the
closure procedure and now reports a converged fixed point at
$M = \qty{1.7716}{\kilo\gram}$, reproduced by its own Stage~6 roll-up to within
\qty{0.1}{\gram}. The figures below have been \textbf{transcribed into the TDD}
(A1, A2, A8 and parts of A4/A5 are closed). They remain provisional because
roughly \qty{34}{\percent} of the mass total is still structural allowance
rather than measured mass.

\begin{table}[h]
\centering
\caption[Values currently produced by the sizing calculator]{Converged values
from \texttt{analysis/SIZING.py}, as transcribed into the TDD. The wheel
diameter is pinned by the packaging envelope
(\texttt{D\_w\_FIXED\_mm}\,=\,120), not chosen by the mass-minimising sweep.}
\label{tab:sizing_current}
\small
\begin{tabular}{l L{5.0cm} c c}
\toprule
Symbol & Quantity & Converged value & Status \\
\midrule
$M$                   & Cube mass (Stage 1 assumption) & \qty{1.7716}{\kilo\gram} & Fixed point \\
$M_{\text{est}}$      & Cube mass (Stage 6 roll-up)    & \qty{1.7716}{\kilo\gram} & $\Delta \approx 0$; 34\% allowance \\
$L$                   & Cube edge length               & \qty{150}{\milli\metre} & Fixed \\
$\eta$                & Energy margin                  & 1.05                    & Chosen \\
$\tau_b$              & Brake torque per wheel         & \qty{5}{\newton\metre}  & Assumed \\
$\tau_g = MgL/2$      & Gravitational tip-over floor   & \qty{1.303}{\newton\metre} & Derived \\
$\beta$               & Brake-amplification factor     & 1.353                   & Derived \\
$\omega_{\max}$       & Max wheel speed                & \qty{6000}{\rpm}        & Agreed with TDD \\
$h_w$                 & Required angular momentum      & \qty{0.2588}{\kilo\gram\metre\squared\per\second} & Derived \\
$I_{w,\text{target}}$ & Target wheel inertia (corner)  & \qty{4.119e-4}{\kilogram\metre\squared} & Derived \\
$I_{w,\text{target}}$ & Target wheel inertia (edge)    & \qty{3.737e-4}{\kilogram\metre\squared} & Not critical \\
$D_w$                 & Wheel outer diameter           & \qty{120}{\milli\metre} & Envelope constraint \\
$N$                   & Ballast stations per wheel     & 15 of $N_{\max}=39$     & Derived \\
$m_w$                 & Wheel mass (each)              & \qty{172.5}{\gram}      & Derived \\
$I_w$                 & Wheel inertia achieved         & \qty{4.599e-4}{\kilogram\metre\squared} & \qty{111.7}{\percent} of target \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{The three script-versus-document conflicts are now closed.}
Resolved as follows, and the document has been populated accordingly.

\begin{enumerate}
  \item \textbf{$\omega_{\max}$: resolved to \qty{6000}{\rpm}}, matching the TDD
        and \texttt{electrical/MOTOR\_SPEED.md}; the script's code has been
        corrected from \qty{5000}{\rpm}. \qty{5000}{\rpm} is retained as a
        documented over-dimensioning check --- it raises $I_{w,\text{target}}$
        by $6/5$, so a wheel closing there closes at \qty{6000}{\rpm} with
        margin. A footnote to \texttt{tab:jumpup\_budget} records this.
  \item \textbf{Wheel geometry: resolved.} \texttt{tab:rw\_params} now carries
        the as-designed ring --- \qty{120}{\milli\metre} OD, \qty{15}{\milli\metre}
        radial width, round M6 through-holes, $N = 15$ on
        $R_{\text{pcr}} = \qty{52.5}{\milli\metre}$,
        $m_w = \qty{172.5}{\gram}$, $I_w = \qty{4.599e-4}{\kilogram\metre\squared}$.
        The diameter is an envelope constraint: left free, the mass-minimising
        sweep prefers a larger ring that does not fit the cube, at a cost of
        \qty{85}{\gram} per wheel.
  \item \textbf{Ballast retention: resolved.} The blind-pocket language has been
        removed from \texttt{06\_testing.tex} (part checks) and
        \texttt{09\_cad.tex} (print settings); the through-bolt in tension is
        the stated retention feature, at \qty{1.47}{\percent} of M6 class~8.8
        proof load and \qty{4.02}{\mega\pascal} bearing stress.
\end{enumerate}

\paragraph{What remains open on the sizing chain.} The residual risk is not a
conflict but a margin asymmetry: the wheel sits \qty{11.7}{\percent} above
target while one station is worth \qty{5.9}{\percent}, so downward ballast trim
is nearly exhausted ($-1$ station) while upward trim is ample ($+24$). A
structure that comes in \emph{lighter} than the \qty{610}{\gram} allowance is
therefore the awkward case, and $\omega_{\max}$ is the relief variable.

\clearpage
\section{TBD items, by owning table}
\label{sec:tbd}

93 of the 97 TBD markers sit in tables. Each block below is one table. Items
marked \closed{} have been populated since the original scan and are retained
for audit; 78 markers remain open.

\subsection{A1 --- Jump-up sizing budget \quad \closed}

\texttt{tab:jumpup\_budget}, \texttt{sections/04\_modeling\_control.tex}.
\textbf{6 TBD --- all populated} from the converged run of
Table~\ref{tab:sizing_current}. The values remain provisional in the sense that
$M$ carries a \qty{34}{\percent} structural allowance, but they are no longer
missing and the table is self-consistent with Stage~6.

\begin{table}[h]
\centering
\small
\begin{tabular}{c l L{6.4cm} L{3.2cm}}
\toprule
Line & Symbol & Quantity & Closed by \\
\midrule
189 & $M$                   & Cube mass                    & CAD mass roll-up \\
191 & $\eta$                & Energy margin                & Design choice (1.02--1.05) \\
193 & $\tau_g = MgL/2$      & Gravitational tip-over floor & Derived from $M$, $L$ \\
194 & $\beta$               & Brake-amplification factor   & Derived from $\tau_b$, $\tau_g$ \\
196 & $h_w$                 & Required angular momentum    & Derived, Eq.~(hw\_ideal) \\
197 & $I_{w,\text{target}}$ & \textbf{Target wheel inertia} & Derived, Eq.~(Iw\_target) \\
\bottomrule
\end{tabular}
\end{table}

Four of the six are pure functions of $M$ and $L$, so this whole block closes
the moment the mass roll-up is real rather than allowance-based.

\subsection{A2 --- Required inertia by contact case \quad \closed}

\texttt{tab:inertia\_cases}, \texttt{sections/05\_hardware\_design.tex}.
\textbf{4 TBD --- all populated.} The calculator now evaluates both contact
cases rather than only the sizing one: edge
$h_w = \qty{0.2348}{\kilo\gram\metre\squared\per\second}$,
$I_{w,\text{target}} = \qty{3.737e-4}{\kilogram\metre\squared}$; corner
\qty{0.2588}{} and \qty{4.119e-4}{} respectively. The corner case demands
\qty{9.3}{\percent} more inertia, which is now shown rather than asserted.

\subsection{A3 --- Rail current budget \quad \ready}

\texttt{tab:power\_budget}, \texttt{sections/05\_hardware\_design.tex},
lines 461--469. \textbf{14 TBD}: typical and worst case for each load.

\begin{table}[h]
\centering
\small
\begin{tabular}{c L{4.6cm} L{6.4cm}}
\toprule
Line & Load & Governing figure \\
\midrule
461 & Teensy 4.1                    & Datasheet \\
462 & XIAO ESP32-C6 telemetry       & TX burst ($\approx$\qty{300}{\milli\ampere}) governs \\
463 & BMI270 IMU                    & Datasheet \\
464 & MG92B brake servo             & Stall current governs \\
465 & MA600 encoders ($3\times$)    & Datasheet \\
467 & \textbf{Logic rail total}     & Checked against LM2596 rating (derates without heatsink) \\
469 & moteus-n1 drivers ($3\times$) & Spin-up, not balance, governs \\
\bottomrule
\end{tabular}
\end{table}

Typical values are datasheet lookups and can be filled today. Worst case is
explicitly defined in the TDD as the \emph{coincident} case --- telemetry burst,
servo stall and full sensor load simultaneously --- not the sum of typicals.

\subsection{A4 --- Driving CAD parameters \quad \blocker}

\texttt{tab:cad\_params}, \texttt{sections/09\_cad.tex}.
\textbf{6 TBD, unchanged.} $L$, $t_w$, $D_w$ and $t_r$ are populated: $D_w$ has
been corrected from \qty{13}{\centi\metre} to the \qty{120}{\milli\metre} of the
as-designed wheel, and the axial thickness $t_r = \qty{5}{\milli\metre}$ added,
since the CAD needs it and the sizing fixes it. The six remaining TBDs are
genuinely open --- they need the packaging study, not the calculator, and the
sizing work does not touch them.

\begin{table}[h]
\centering
\small
\begin{tabular}{c l L{7.6cm}}
\toprule
Line & Symbol & Parameter \\
\midrule
34 & $d_m$      & Motor axis offset from cube centre \\
35 & $h_m$      & Motor mounting face height on its axis \\
36 & $c_w$      & Wheel-to-subframe axial clearance \\
37 & $c_o$      & Clearance between orthogonal wheel modules \\
38 & $V_{\min}$ & \textbf{Minimum internal clear volume} \\
39 & $r_c$      & Corner contact feature radius \\
\bottomrule
\end{tabular}
\end{table}

$V_{\min}$ is step~2 of the closure procedure and therefore the head of the
whole dependency chain: it fixes $L$, which fixes $M$, which fixes
$I_{w,\text{target}}$. $c_w$ and $c_o$ are additionally consumed by the
interference checks of the CAD verification stage.

\subsection{A5 --- CAD model tree and part list \quad \ready}

\texttt{tab:cad\_parts}, \texttt{sections/09\_cad.tex}, lines 65--89.
\textbf{35 TBD}: fabrication route and status for every part, plus five missing
quantities.

\begin{table}[h]
\centering
\small
\begin{tabular}{c l L{5.0cm} L{4.6cm}}
\toprule
Line & Ref & Part & Missing \\
\midrule
65 & M1 & Motor subframe                & Qty, route, status \\
66 & M2 & Motor mount ($\times$ axis)   & Route, status \\
67 & M3 & Battery cradle / retention    & Route, status \\
68 & M4 & Electronics tray              & Route, status \\
69 & M5 & Brake servo bracket           & Route, status \\
73 & W1 & Ballasted ring                & Route, status \\
74 & W2 & Wheel hub / rotor adapter     & Route, status \\
75 & W3 & Ballast bolt and nut set      & Qty, status \\
76 & W4 & Brake strike feature          & Route, status \\
80 & F1 & Frame member / edge rail      & Qty, route, status \\
81 & F2 & Corner contact feature        & Route, status \\
82 & F3 & Face panel / wheel guard      & Qty, route, status \\
86 & R1 & Face plate (1-DoF rig)        & Route, status \\
87 & R2 & Pivot bearing mount           & Route, status \\
88 & R3 & Rig motor mount               & Route, status \\
89 & R4 & Hard stops / containment      & Qty, route, status \\
\bottomrule
\end{tabular}
\end{table}

W3's quantity is $3N$ where $N$ is the ballast count. With the wheel geometry
now settled at $N = 15$ per wheel, this has been committed as \textbf{45} M6
bolt-and-nut sets, leaving 34 TBD in this table. The rest are fabrication routes
and statuses, which are decidable today and do not depend on the sizing.

\subsection{A6 --- Print and manufacturing settings \quad \ready}

\texttt{tab:print\_settings}, \texttt{sections/09\_cad.tex}, lines 138--143.
\textbf{18 TBD}: material, infill/walls and orientation for W1, W2, M1, M2, F1,
F2 --- the six structural printed parts.

The TDD states the requirement these settings must meet: the wheel ring carries
centrifugal load at the ballast holes and the motor mounts carry the brake
impulse, so orientation is specified relative to the principal load direction
and a part reprinted differently is not the part that was analysed. The
calculator assumes PET-CF at \qty{1290}{\kilo\gram\per\metre\cubed}, which
should be the material entry for W1 at minimum.

\subsection{A7 --- Mass properties verification \quad \waiting}

\texttt{tab:mass\_verification}, \texttt{sections/09\_cad.tex}, lines 197--200.
\textbf{12 TBD}: CAD value, measured value and discrepancy for total cube mass
$M$, wheel mass $m_w$, wheel inertia $I_w$ and CoM offset.

The CAD column can be filled as soon as the model exists; the measured column
requires the assembled hardware (assembly step~8). This is the table that closes
the mass loop, so a discrepancy here re-enters $I_{w,\text{target}}$.

\subsection{A8 --- Pre-fabrication feasibility gate \quad \closed}

\texttt{tab:jumpup\_gate}, \texttt{sections/06\_testing.tex}.
\textbf{8 TBD --- all populated}, and the gate passes on every row:
$\omega_w = \qty{5374}{\rpm}$ against $\omega_{\max} = \qty{6000}{\rpm}$ for a
speed margin of \num{0.896}; $I_w = \qty{4.599e-4}{}$ against
$I_{w,\text{target}} = \qty{4.119e-4}{}$, a \qty{+11.7}{\percent} route
disagreement; trim authority $-1/+24$ stations; retention at
\qty{1.47}{\percent} of proof load. Row~116 has also been reworded off the
superseded blind-pocket scheme, as flagged below.

\begin{table}[h]
\centering
\small
\begin{tabular}{c L{4.8cm} L{6.4cm}}
\toprule
Line & Quantity & Pass condition \\
\midrule
109 & Required wheel speed $\omega_w$        & $\leq$ achievable speed with margin \\
110 & Achievable wheel speed $\omega_{\max}$ & Sustained in service, not no-load \\
111 & Speed margin $\omega_w/\omega_{\max}$  & $<1$ by a stated margin \\
112 & Target inertia $I_{w,\text{target}}$   & --- \\
113 & Achieved inertia $I_w$                 & $\geq I_{w,\text{target}}$ \\
114 & Route disagreement                     & Reported; explained if large \\
115 & Ballast trim authority                 & Non-zero in both directions \\
116 & Nut retention margin                   & Retention load carried by a stated feature \\
\bottomrule
\end{tabular}
\end{table}

Row 116 was written against the superseded blind-pocket scheme; it has been
reworded to ``Ballast retention margin'' with the pass condition stated on bolt
tension and head/nut bearing stress, which is what the through-hole design
actually relies on. The same correction was applied to the part-check paragraph
of \texttt{06\_testing.tex} and the print-settings preamble of
\texttt{09\_cad.tex}, both of which still referred to pockets and a retention
floor.

\subsection{A9 --- 1-DoF plant identification \quad \waiting}

\texttt{tab:paramid\_methods}, \texttt{sections/06\_testing.tex},
lines 307--310. \textbf{4 TBD}: torque constant $K_t$, body inertia $J$,
friction, loop latency. Methods and instruments are already specified --- only
the measured values and their tolerances are pending. Requires the rig
(gate M6).

\subsection{A10 --- Control performance metrics \quad \waiting}

\texttt{tab:perf\_metrics}, \texttt{sections/06\_testing.tex}, lines 405--410.
\textbf{5 TBD}: targets for recovery angle, settling time,
disturbance-rejection bandwidth, slew tracking error, momentum drift. Control
effort is already defined by reference to the driver limits and carries no TBD.

\subsection{A11 --- Connector schedule \quad \ready}

\texttt{tab:connectors}, \texttt{sections/10\_electrical\_detail.tex},
lines 78--88. \textbf{11 TBD}: housing and keying for J1--J11.

The governing rule is already stated in the TDD: no two connectors carrying
different voltages may share a housing type, so a mis-mate is impossible by
construction. J1 is fixed by the BOM (XT90). The remaining ten are open choices
and can be decided today.

\subsection{A12 --- Wire schedule \quad \ready}

\texttt{tab:wire\_schedule}, \texttt{sections/10\_electrical\_detail.tex},
lines 112--118. \textbf{14 TBD}: gauge and length for H1--H7. The sizing basis
is already stated per row (peak bus current for H1--H2, per-axis spin-up for H3,
motor phase current for H4, topology for H5, length limit for H6, stall current
for H7). Gauge follows from A3; length follows from the CAD envelope.

\subsection{A13 --- As-built electrical measurements \quad \waiting}

\texttt{tab:electrical\_measured},
\texttt{sections/10\_electrical\_detail.tex}, lines 200--206. \textbf{7 TBD}:
$K_m$, $I_w$ from spin-down, viscous and Coulomb friction, command-to-encoder
latency, achievable wheel speed on the bus, worst-case logic rail load, runtime
per charge. Each row already names the estimate it replaces. Requires hardware.

\section{TBD in prose}

Four TBD markers sit in running text rather than in a table.

\begin{table}[h]
\centering
\small
\begin{tabular}{L{4.4cm} L{9.2cm}}
\toprule
Location & Pending \\
\midrule
\texttt{06\_testing.tex}:12  & Generic statement that thresholds are TBD. Resolves itself when the tables close; no separate action. \\
\texttt{06\_testing.tex}:202 & \textbf{Robustness sweep magnitude} --- how far plant parameters are perturbed about nominal. Set once the identification tolerances of A9 are known. \\
\texttt{06\_testing.tex}:355 & \textbf{Edge-balance numeric targets} beyond the G6 gate criterion. \\
\texttt{06\_testing.tex}:369 & \textbf{Slew tracking targets}. \\
\bottomrule
\end{tabular}
\end{table}

\section{Empty sections}

\begin{table}[h]
\centering
\small
\begin{tabular}{L{4.6cm} L{4.0cm} L{5.0cm}}
\toprule
Section & File & State \\
\midrule
\textbf{Appendix D --- Control Algorithms} & \texttt{11\_control\_algorithms.tex} & \blocker{} \textbf{0 bytes.} Input by \texttt{main.tex}:151 and cross-referenced six times as \texttt{app:control\_algorithms} from Sec.~5 and Sec.~6 \\
\S7.1 NMPC \& warm-start control   & \texttt{07\_future\_work.tex}:4     & Heading only \\
\S7.2 Autonomous stand-up          & \texttt{07\_future\_work.tex}:6     & Heading only \\
\S7.4 Single-wheel variant         & \texttt{07\_future\_work.tex}:262   & Heading + comment \\
\bottomrule
\end{tabular}
\end{table}

Appendix~D is the most serious content gap in the document: six live
cross-references point into an empty file. The TDD promises it will contain the
mode state machine, the estimator and controller in executable pseudocode, the
fixed gains and tuning parameters, the timing budget and the fault responses.

The three empty Future Work headings should either be written or deleted ---
empty subsections in a submitted document read worse than a shorter list.

\section{Comment-only stubs}

Twenty subsections exist only as LaTeX comments describing what should be there.

\subsection*{Section 3 --- System Analysis \& Requirements \quad \blocker}

Three of four subsections are stubs. At roughly 230 words this section is a hole
in the middle of the document and is more conspicuous to an assessor than any
amount of over-length prose elsewhere.

\begin{table}[h]
\centering
\small
\begin{tabular}{c L{5.0cm} L{7.0cm}}
\toprule
Line & Subsection & What the comment asks for \\
\midrule
7  & \S3.1 Scope of work \& use cases      & Boundaries of the design effort; the four target behaviours as primary use cases \\
14 & \S3.2 Actuation trade-off             & RW-vs-CMG justification. Prose exists; the comment is unresolved \\
48 & \S3.4 Coordinate frames \& conventions & Body frame, world frame, sign conventions for tilt and wheel angle \\
\bottomrule
\end{tabular}
\end{table}

\subsection*{Mechanical}

\begin{table}[h]
\centering
\small
\begin{tabular}{L{4.6cm} L{9.0cm}}
\toprule
Location & Content \\
\midrule
\texttt{05\_hardware\_design.tex}:204 & 2D prototype structural concept \\
\texttt{05\_hardware\_design.tex}:586 & \S5.5 System Integration --- ``TBD pending CAD'' \\
\texttt{09\_cad.tex}:97, 101          & 2D prototype construction notes; rig general-assembly figure \\
\texttt{09\_cad.tex}:107, 111, 112    & 3D cube drawings; exploded view; section view through two orthogonal wheel modules \\
\bottomrule
\end{tabular}
\end{table}

\subsection*{Electrical --- eleven figure placeholders}

\texttt{sections/10\_electrical\_detail.tex}: schematic sheets 1--7 at lines 18,
25, 32, 38, 44, 50 and 55; harness drawing and as-built photograph at lines 123
and 125; perfboard floorplan and board outline drawing at lines 130 and 133.

Each placeholder already names the constraint the sheet must be verified
against, so the drawing brief is written --- only the drawings are missing.

\section{Missing figures}

Twenty-one figures are referenced or described but not present: the twenty
placeholders above, plus one more.

\begin{itemize}
  \item \texttt{04\_modeling\_control.tex}:21--22 --- free-body diagram of the
        2D pendulum, labelling $\theta_b$, $\theta_w$, $l$, $l_b$ and the pivot.
        This is the only figure suggested in Sec.~4, and that section currently
        has no figures at all.
\end{itemize}

\section{Defects found alongside the TBDs}

These are not open items in the same sense --- they are errors in content that
is already written, and each is a short fix.

\begin{table}[h]
\centering
\small
\begin{tabular}{c L{4.2cm} L{8.4cm}}
\toprule
\# & Location & Defect \\
\midrule
1 & \texttt{05\_hardware\_design} :420 vs \texttt{08\_bom}:86 & Battery capacity contradiction: \qty{3600}{\milli\ampere\hour} in the power architecture, \qty{1550}{\milli\ampere\hour} in the BOM, same 6S pack. The sizing script uses 3600. \\
2 & \texttt{05\_hardware\_design} & \closed{} \texttt{tab:rw\_params} was missing its \texttt{\textbackslash caption} and would not have appeared in the List of Tables. A caption is now present. \\
3 & \texttt{06\_testing}:65--66 vs \texttt{tab:gates} & G6 hard-kill date stated as 13~August; Table~\texttt{tab:gates} puts G6 at 17~August. A kill date cannot precede the gate it kills. \\
4 & \texttt{02\_project\_organization} \texttt{\_tasks}:46 & Task B4 says ``Raspberry Pi software architecture''; Sec.~5 and the BOM specify Teensy~4.1 throughout. \\
5 & \texttt{08\_bom}:64 vs \texttt{tab:cad\_parts} & Brake servo quantity: BOM says 1--3, the CAD part list says 3 brackets, Sec.~5 says ``servos'' plural. \\
6 & Author list vs \texttt{tab:team\_roles} vs task table & \closed{} Name spelling was inconsistent (``Niccol\`o'' / ``Niccolo'' / ``Nicc''). The report now uses the unaccented ``Niccolo'' throughout: author list, cover, roles table and task table. \\
7 & \texttt{02\_project\_organization} \texttt{\_tasks}:79--82 & Task IDs C11--C14 appear under the ``3D Design'' block, after D1--D7. \\
8 & All section files & British/American spelling mixed: realise/realize, characterise/characterize, behaviour/behavior, sometimes within one section. \\
9 & \texttt{04\_modeling\_control}:131 & Hard-coded \texttt{\textbackslash newpage} splits a sentence across pages mid-argument. \\
10 & All tables & \closed{} Every float used \texttt{[h]}; all are now \texttt{[htbp]}. No \texttt{[h]} float remains in \texttt{sections/}. \\
11 & \texttt{sections/} & \texttt{code.cpp} and \texttt{code.exe} are untracked in the repo; a compiled binary should not be committed. \\
\bottomrule
\end{tabular}
\end{table}

\section{Suggested order of work}

\begin{enumerate}
  \item \closed{} \textbf{The three script-versus-document conflicts} of
        Section~\ref{sec:chain} are resolved --- $\omega_{\max}$ at
        \qty{6000}{\rpm}, the wheel table rebuilt on the as-designed
        \qty{120}{\milli\metre} through-hole ring, and the blind-pocket
        retention argument removed. A1, A2 and A8 were then populated from the
        converged run (19 TBD closed).
  \item \textbf{Close the packaging study} (A4) --- now the head of the
        register. $V_{\min}$ and the clearances release $L$, then $M$. Note
        that A1, A2 and A8 are populated but \emph{provisional}: they rest on a
        \qty{610}{\gram} structural allowance, so closing A4 will move them
        again. That is the next turn of the fixed-point iteration, not a
        re-opening of settled items.
  \item \textbf{Write Appendix~D}, or remove the six cross-references that point
        into it.
  \item \textbf{Write Section~3}, which is three-quarters empty.
  \item \textbf{Do the \ready{} items}, which need no upstream data: A3 (rail
        budget), A5 (fabrication routes), A6 (print settings), A11 (connectors),
        A12 (wire gauges).
  \item \textbf{Fix the eleven defects}, which cost credibility out of
        proportion to the effort of fixing them.
  \item \textbf{The remaining \waiting{} items} (A7, A9, A10, A13 and the four
        prose TBDs) close on hardware and cannot be accelerated on paper.
\end{enumerate}

\end{document}
