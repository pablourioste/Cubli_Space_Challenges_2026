% ================= 5. PRELIMINARY HARDWARE DESIGN =================
\section{Preliminary Hardware Design}
\label{sec:hardware}

This design is preliminary in a specific sense. The actuation, sensing and power
components are selected (Appendix~\ref{app:bom}) and the principal dimensions
follow from the closure of Section~\ref{sec:massbudget}: a
\qty{180}{\milli\metre} cube with three \qty{130}{\milli\metre} ballasted
reaction wheels at a \qty{6000}{\rpm} design speed. What remains open is the
structure that carries them, and two inputs on which those dimensions depend ---
the total mass $M$ and the brake torque $\tau_b$ --- are not yet measured. The
architecture is therefore kept parametric so that the closure can be re-run
against measured values rather than re-derived
(Section~\ref{sec:cad_method}), and the two open inputs are flagged wherever
they are used rather than presented as settled.

\subsection{System Architecture}
\label{sec:architecture}

Figure~\ref{fig:system_loop} shows the closed control loop that ties the four
subsystems together. The Teensy~4.1 runs estimation and control at
\qty{1}{\kilo\hertz} and issues torque commands over CAN-FD to three moteus-n1
drivers, each closing a \qty{30}{\kilo\hertz} field-oriented current loop on its
MN4006 motor. The resulting reaction torque acts on the cube body; the body's
attitude is sensed by the BMI270 IMU and the rotor angle by the MA600 absolute
encoder, closing the loop back to the controller. The two loops run at different
rates by design: the fast current loop is delegated entirely to the drivers, so
the flight controller need only meet the \qty{1}{\milli\second} outer-loop
deadline of Section~\ref{sec:control2d}.

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{images/cubli_full_system_loop.png}
\caption{System architecture and control loop. Blue: processing; green: sensing;
orange: actuation. Rates shown are the design targets of
Section~\ref{sec:modeling}.}
\label{fig:system_loop}
\end{figure}

\subsection{Sizing Constraints and Closure Procedure}
\label{sec:massbudget}

The cube edge length $L$ and the reaction-wheel inertia $I_w$ are not
independent design choices: they are the two ends of a single constraint chain
that begins with what must physically be carried inside the cube. This section
states that chain, the order in which it is closed, and the envelope it
selects: $L = \qty{180}{\milli\metre}$, evaluated in
Section~\ref{sec:envelope_selection}. The numbers follow from the procedure
rather than preceding it, and the closure is re-run whenever an input moves.

\paragraph{The battery and the internal structure drive the envelope.} The
dominant volume claim inside the cube is not the reaction wheels but the power
and support hardware. A LiPo pack sized for the jump-up duty cycle is a
monolithic, incompressible block: unlike the electronics it cannot be
redistributed into the corners or thinned, and its dimensions follow from the
required capacity and discharge rating rather than from any packaging
preference. Around it must sit an \emph{inner structure}---a rigid subframe
that locates the three orthogonal motors on mutually perpendicular axes through
a common centre, carries the three motor drivers, the flight controller, the IMU
and the brake servos, and reacts the motor mounting loads into the outer
frame. The three wheel/motor modules must not intersect one another, the pack,
or this subframe, and the pack should sit near the geometric centre so that it
does not skew the cube's centre of mass away from the body diagonal.

The minimum internal clear volume is therefore set by the pack, the subframe and
the electronics stack together, and $L$ is the smallest edge length that
contains that volume plus the wall thickness and the corner and edge contact
features. This is the first quantity to fix, and everything downstream depends
on it.

\paragraph{The envelope caps the wheel, and the wheel must meet the momentum
requirement.} Once $L$ is set, the largest wheel outer diameter $D_w$ that
clears the subframe, the motor body and the adjacent orthogonal modules is a
geometric consequence, and it caps the achievable inertia---not only through
$I_w \sim m_w D_w^2/4$, but because any ballast necessarily sits inboard of the
ring it displaces, so a wheel that is short of target at its maximum diameter
cannot be rescued by adding mass. Against this stands the requirement from the
jump-up analysis (Section~\ref{sec:jumpup_target}),
\[
I_{w,\text{target}} = \frac{h_w(M, L)}{\omega_{\max}},
\qquad
h_w \propto M L^{3/2},
\]
which grows with both the cube mass and the edge length. Enlarging the cube to
gain wheel diameter therefore also raises the requirement---but the achievable
inertia grows faster than the requirement does ($D_w^2$ against $L^{3/2}$), so
the loop does converge, and a wheel that is short at a given $L$ can be closed
by a modest increase in $L$.

\paragraph{Mass closes the loop.} The cube mass $M$ is not merely a logistics
figure: it re-enters the dynamics through the gravitational tip-over torque
$\tau_g = MgL/2$, which sets the floor the brake torque must exceed, and through
$h_w \propto M$. Growing the envelope to fit a larger wheel adds frame and wheel
mass, which raises $I_{w,\text{target}}$ again. Mass, volume and inertia are thus
one coupled system, closed by the iteration of Table~\ref{tab:sizing_procedure}
rather than by any of the three in isolation.

\begin{table}[h]
\centering
\caption{Sizing closure procedure. Steps are executed in order; a failure at
step 4 returns to step 3 or step 1 as indicated.}
\label{tab:sizing_procedure}
\small
\begin{tabular}{c L{4.6cm} L{6.4cm}}
\toprule
Step & Determines & Governing consideration \\
\midrule
1 & Battery pack geometry & Capacity and discharge rating for the jump-up duty cycle \\
2 & Inner clear volume $V_{\min}$, hence minimum $L$ & Pack $+$ motor subframe $+$ driver/controller stack, non-intersecting; pack near centre \\
3 & Maximum wheel diameter $D_w$, hence achievable $I_w$ & Clearance to subframe, motor body and the two orthogonal modules \\
4 & Requirement $I_{w,\text{target}} = h_w(M,L)/\omega_{\max}$ & Corner jump-up, Eqs.~\eqref{eq:hw_ideal}--\eqref{eq:Iw_target} \\
5 & Accept or iterate & If $I_w < I_{w,\text{target}}$: raise $L$ (step 2), raise $\omega_{\max}$, or trim $M$; re-enter at step 3 \\
6 & Mass reconciliation & Updated $M$ feeds back into step 4; iterate to a fixed point \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Inertia requirement is corner-critical.} The two contact cases are
not equally demanding. The corner equilibrium requires a larger rise of the
centre of mass and a less favourable momentum-transfer geometry than the edge
equilibrium, so for a common maximum wheel speed $\omega_{\max}$ it demands the
greater wheel inertia (Table~\ref{tab:contact_factors}). Step~4 above is
therefore always evaluated for the corner case; a wheel that satisfies the
corner requirement satisfies the edge requirement with margin.
Table~\ref{tab:inertia_cases} records both once the procedure closes.

\begin{table}[h]
\centering
\caption{Required per-wheel inertia by contact case, at
$M = \qty{1.40}{\kilo\gram}$, $L = \qty{180}{\milli\metre}$ and
$\omega_{\max} = \qty{6000}{\rpm}$. The corner case governs by
\qty{10.2}{\percent}.}
\label{tab:inertia_cases}
\begin{tabular}{lcc}
\toprule
Contact case & $h_w$ (kg\,m$^2$/s) & $I_{w,\text{target}}$ ($10^{-4}$\,kg\,m$^2$) \\
\midrule
Edge                       & 0.2395 & 3.812 \\
\textbf{Corner (critical)} & \textbf{0.2640} & \textbf{4.202} \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Envelope Selection: Why \qty{180}{\milli\metre}}
\label{sec:envelope_selection}

Running the procedure over candidate edge lengths selects
$L = \qty{180}{\milli\metre}$. Two independent arguments converge on it, and
both are needed --- the dynamic one alone would not settle the question.

\paragraph{The inertia budget.} Table~\ref{tab:envelope_trade} evaluates step~4
against step~3 for each candidate. The requirement grows as $ML^{3/2}$ and is
further penalised by $\beta$, which degrades with $L$; against this, the largest
wheel the frame admits is roughly $L$ less a \qtyrange{30}{40}{\milli\metre}
allowance for wall thickness, motor boss and contact features, and the inertia
it supplies grows as the square of that diameter. At \qty{150}{\milli\metre} the
largest wheel that fits is about \qty{120}{\milli\metre} in diameter and
supplies $2.63\times10^{-4}$\,kg\,m$^2$ against a
$3.03\times10^{-4}$\,kg\,m$^2$ requirement --- \qty{13}{\percent} short, and
\emph{not} recoverable by ballast, since ballast necessarily sits inboard of the
ring it displaces. At \qty{180}{\milli\metre} the requirement is
$4.20\times10^{-4}$\,kg\,m$^2$ and a \qty{130}{\milli\metre} wheel supplies
$3.49\times10^{-4}$\,kg\,m$^2$ from its printed structure alone, closing the
remainder with ballast and retaining trim authority in both directions.

\begin{table}[h]
\centering
\caption{Envelope trade at $\omega_{\max} = \qty{6000}{\rpm}$,
$M = \qty{1.40}{\kilo\gram}$. The wheel cap assumes a
\qty{30}{\milli\metre} frame allowance; bare-ring inertia is for the
\qtyproduct{20 x 12}{\milli\metre} ring section of Section~\ref{sec:rw_sizing}.}
\label{tab:envelope_trade}
\small
\begin{tabular}{ccccl}
\toprule
$L$ (mm) & $\beta$ & $I_{w,\text{target}}$ ($10^{-4}$) & Bare ring at cap ($10^{-4}$) & Verdict \\
\midrule
150 & 1.259 & 3.03 & 2.63 & Short by \qty{13}{\percent} \\
160 & 1.282 & 3.40 & 3.49 & Closes with no trim margin \\
\textbf{180} & \textbf{1.328} & \textbf{4.20} & \textbf{5.73} & \textbf{Clears; \qty{130}{\milli\metre} wheel selected} \\
200 & 1.379 & 5.11 & 8.78 & Clears, but heavier and $\beta$ worse \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Packaging, which is independently decisive.} The dynamic argument
alone would leave \qty{160}{\milli\metre} arguable. Packaging does not: the
power and signal board is \qtyproduct{150 x 90}{\milli\metre}, so it does not
fit inside a \qty{150}{\milli\metre} envelope at any wall thickness, and the
interior must additionally carry three motors, three drivers, a 6S pack and the
brake servos (Section~\ref{sec:layout}). This is the constraint that removes the
smaller options outright, and it is why step~2 of
Table~\ref{tab:sizing_procedure} precedes the wheel sizing rather than following
it.

\paragraph{What the decision costs.} A larger cube is not free, and the penalty
should be stated rather than buried. Frame mass grows with $L$, and $M$ is
already the least comfortable number in the budget
(Table~\ref{tab:massbudget}); $\beta$ degrades from \num{1.259} at
\qty{150}{\milli\metre} to \num{1.328} at \qty{180}{\milli\metre}, so the finite
brake loses a larger fraction of its impulse to gravity; and both effects feed
back into $I_{w,\text{target}}$. Note that $\beta$ depends entirely on the
unverified brake torque of Section~\ref{sec:brake_torque}, so this column of the
trade is the least secure part of it. The design is accepted at
\qty{180}{\milli\metre} because the alternative does not close at all, not
because the cost is negligible.

This makes the cube larger than the \qty{150}{\milli\metre} ETH prototype
referenced in Section~\ref{sec:background}, and the reason is material rather
than dimensional: that design used a machined steel flywheel, where this one
uses a printed ring with steel ballast at a substantially lower achievable
density at radius. The lost density is compensated with radius, and radius is
bought with envelope.

\paragraph{Mass budget.} The allocation of Table~\ref{tab:massbudget} is the
bookkeeping side of steps 5 and 6: it is populated as components are selected
and re-totalled on each iteration, since the total is an input to
$I_{w,\text{target}}$ and not merely an output of the design.

\begin{table}[h]
\centering
\caption{Mass budget (per cube). Known figures are from the selected components;
allowances are provisional. The total is an input to the inertia requirement,
not only a result --- see the reconciliation below.}
\label{tab:massbudget}
\small
\begin{tabular}{l c l}
\toprule
Subsystem & Mass (g) & Basis \\
\midrule
Battery pack                  & 600 & Selected 6S pack \\
Reaction wheels ($3\times$)   & 468 & Sec.~\ref{sec:rw_sizing}, \qty{156}{\gram} each \\
Motors ($3\times$)            & 204 & Datasheet, \qty{68}{\gram} each \\
Drivers ($3\times$)           & 44  & Datasheet, \qty{14.6}{\gram} each \\
\midrule
\textit{Subtotal, known}      & \textit{1316} & \textit{Fixed by selection} \\
\midrule
Inner structure / motor subframe & \textcolor{red}{TBD} & Pending CAD \\
Outer frame / contact features & \textcolor{red}{TBD} & Pending CAD \\
Electronics (MCU, board, IMU, servos) & \textcolor{red}{TBD} & Allowance, pending weigh-in \\
Wiring / fasteners / margin   & \textcolor{red}{TBD} & Allowance \\
\midrule
\textbf{Design value $M$}     & \textbf{1400} & \textbf{Used in Sec.~\ref{sec:jumpup_target}} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{The mass budget does not currently close, and this is the leading
design risk.} The four selected components above total \qty{1316}{\gram},
which is \qty{94}{\percent} of the \qty{1400}{\gram} figure that
Section~\ref{sec:jumpup_target} uses as an input --- with no frame, no
electronics board, no wiring, no fasteners and no brake hardware yet counted.
The remaining allowance of \qty{84}{\gram} is not credible for those items.

This is precisely the fixed-point failure that Section~\ref{sec:background}
identifies as the characteristic way Cubli replications fail, and the honest
statement is that the design is currently on the wrong side of it. The coupling
makes it worse than a simple overrun: $h_w \propto M$, so a realistic $M$ raises
$I_{w,\text{target}}$, which demands a heavier wheel, which raises $M$ again.
Three responses are available, in order of preference:

\begin{enumerate}
  \item \textbf{Reduce structural mass.} The frame and subframe are the only
        large uncommitted allocations, and they are the parts the CAD can still
        influence. This is the first recourse and it is why the mass roll-up is
        an output of the model (Section~\ref{sec:cad_method}) rather than an
        annotation on it.
  \item \textbf{Accept a higher $M$ and re-close.} The wheel retains upward trim
        authority (Section~\ref{sec:rw_sizing}), so a moderate increase can be
        absorbed by installing additional ballast rather than by re-designing.
        The margin available for this is quantified there and it is not large.
  \item \textbf{Raise $\omega_{\max}$.} Since $I_{w,\text{target}} \propto
        1/\omega_{\max}$, recovering the deliberate conservatism of
        Section~\ref{sec:jumpup_target} is the relief valve of last resort. It
        is listed last because it trades away the discharge-curve robustness
        that motivated the choice, and every load in the machine scales with
        $\omega_{\max}^2$.
\end{enumerate}

The measured mass at first assembly is therefore a gating figure, not a
reporting one: Appendix~\ref{app:cad_mass} records it against the model, and the
closure of Table~\ref{tab:sizing_procedure} is re-run on that number before the
wheels are considered frozen.

\subsection{Mechanical Design and CAD Construction}
\label{sec:mechanical}

This subsection states the mechanical concept and the CAD conventions the build
follows. It is deliberately kept to the level of decisions and interfaces:
the part-by-part model tree, the manufacturing drawings, the fabrication
settings and the assembly sequence are recorded in
Appendix~\ref{app:cad}, so that a change to a bracket does not require an
edit to the design rationale, and the rationale is not buried in geometry.

\subsubsection{CAD Methodology and Model Conventions}
\label{sec:cad_method}

The model is built top-down from the envelope closure of
Section~\ref{sec:massbudget} rather than bottom-up from individual parts, so
that the edge length $L$ and the wheel diameter $D_w$ propagate to every
dependent feature when an iteration changes them. Three conventions make that
propagation reliable, and they are stated here because they constrain how every
part in Appendix~\ref{app:cad} is modelled:

\begin{enumerate}
  \item \textbf{A single skeleton drives the assembly.} $L$, $D_w$, the motor
        axis offsets and the wall thickness live in one parameter table
        referenced by every part; no downstream part carries a hard-coded
        dimension that duplicates one of them.
  \item \textbf{The body frame is the CAD origin.} The assembly origin is the
        cube's geometric centre, with axes on the three motor spin axes, so
        that CAD-reported mass properties are directly the $I$ and $r_{cm}$ of
        Table~\ref{tab:3d_vars} without a change of basis.
  \item \textbf{Mass properties are an output of the model, not an annotation.}
        Every part carries its assigned material and print density, so the
        assembly mass roll-up is the quantity fed back into
        Table~\ref{tab:massbudget} at step~6 of
        Table~\ref{tab:sizing_procedure}.
\end{enumerate}

Convention 3 is what makes the iteration auditable: the mass budget and the
inertia requirement are read from the same model, so a design that closes on
paper cannot silently fail to close in the CAD.

\subsubsection{2D Prototype Build}
\label{sec:cad_2d}

% Structural concept of the single-face rig: 3D-printed plate, central
% motor/wheel mount, corner bearing, and servo brake (cross-reference
% Sec.~\ref{sec:2dmodel}).

The single-face rig is the first article built and the bench on which the
parameters of Table~\ref{tab:params} are measured. Its structure is a
3D-printed plate sized to one cube face, a central motor and wheel mount, a
corner bearing defining the single rotational degree of freedom, and the servo
brake. Detailed geometry and the drawing set are in
Appendix~\ref{app:cad_2d}.

\subsubsection{3D Cube Frame}

The frame is conceived in two parts, reflecting the constraint chain of
Section~\ref{sec:massbudget}. An \emph{inner structure} locates the three motors
on mutually perpendicular axes through a common centre and carries the battery
pack, the three drivers, the flight controller and the brake servos; it is the
element that fixes the minimum internal clear volume and therefore the edge
length $L$. An \emph{outer frame} carries the edge and corner contact features,
protects the wheels, and reacts the motor mounting loads from the inner
structure into the ground contact. Detailed geometry is pending CAD and follows
the envelope closure.

The split is also the main mechanical interface in the machine, and it is
managed as one: the inner structure is designed against a fixed bolt pattern and
keep-out envelope published to the outer frame, so the two can be revised
independently as long as that interface holds. The controlled interfaces are
listed in Table~\ref{tab:mech_interfaces}; the parts realising them, with their
drawings, are in Appendix~\ref{app:cad_3d}.

\begin{table}[h]
\centering
\caption{Controlled mechanical interfaces. Each is frozen once agreed, so that
the parts on either side can be revised independently.}
\label{tab:mech_interfaces}
\small
\begin{tabular}{L{3.6cm} L{3.6cm} L{6.0cm}}
\toprule
Interface & Between & Controlled quantities \\
\midrule
Motor mount        & Motor $\leftrightarrow$ inner structure & Bolt circle, shaft height, concentricity to the wheel axis \\
Wheel hub          & Wheel $\leftrightarrow$ motor rotor     & Bore, bolt pattern, axial location, balance datum \\
Frame joint        & Inner structure $\leftrightarrow$ outer frame & Bolt pattern, keep-out envelope, load path to the contact features \\
Electronics mount  & Board $\leftrightarrow$ inner structure & Board outline, mounting-hole pattern, keep-out heights, connector exit directions (Sec.~\ref{sec:layout}) \\
Brake mount        & Servo $\leftrightarrow$ inner structure & Servo location, barrier throw, clearance to the ballast bolt heads \\
Battery retention  & Pack $\leftrightarrow$ inner structure & Pack envelope, retention method, centring tolerance to the cube centre \\
Contact features   & Outer frame $\leftrightarrow$ ground    & Corner and edge geometry, material, replaceability \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Reaction-Wheel Sizing}
\label{sec:rw_sizing}

The wheel must supply the per-wheel inertia $I_{w,\text{target}}$ demanded by
the corner jump-up (Section~\ref{sec:jumpup_target}), at an outer diameter no
greater than the envelope permits (step~3 of
Table~\ref{tab:sizing_procedure}), while remaining light enough not to dominate
the cube mass---since its own mass feeds back into $I_{w,\text{target}}$
through $M$. These three demands pull against each other, and the wheel
architecture is chosen so that the design can be trimmed against them after
fabrication rather than having to be right the first time.

\paragraph{Architecture: ballasted ring.} Rather than adjusting inertia by
scaling a solid disc, the wheel is built as a printed \emph{ring plus spokes}
whose inertia is then tuned by \emph{ballast}: standard steel bolts and nuts
seated in blind hexagonal pockets distributed around the ring. Each pocket both
removes plastic and adds steel at the largest available radius, so the net
inertia gain per pocket is large, and the target is reached by choosing how many
bolts to install. This makes wheel inertia a discrete, reversible knob: the
physical wheel can be re-tuned in either direction after fabrication to absorb
the parameter uncertainty that the sizing procedure necessarily carries---most
importantly the residual uncertainty in $M$, which is not known until the build
is complete. A solid disc offers no such adjustment.

The design intent is that the bare printed ring supplies most of the required
inertia and the ballast acts only as a trim, so that the bolt count sits well
inside its feasible range in both directions.

\paragraph{Parameters to be fixed.} The geometry is defined by the quantities
of Table~\ref{tab:rw_params}, all of which follow from the envelope closure of
Section~\ref{sec:massbudget} and are recorded there once fixed.

\begin{table}[h]
\centering
\caption{Reaction-wheel design parameters at the selected geometry. Ballast is
standard M6 hex nuts (ISO 4032) in blind hexagonal pockets; $m_b$ and
$\Delta I_b$ are net of the plastic each pocket displaces.}
\label{tab:rw_params}
\small
\begin{tabular}{l l c}
\toprule
Symbol & Parameter & Value \\
\midrule
$D_w$      & Ring outer diameter (capped by envelope) & \qty{130}{\milli\metre} \\
$w_r$      & Ring radial width                         & \qty{20}{\milli\metre} \\
$t_r$      & Ring axial thickness                      & \qty{12}{\milli\metre} \\
$n_s, w_s$ & Spoke count and width                     & 3, \qty{10}{\milli\metre} \\
$\rho_p$   & Printed-material density (PET-CF)         & \qty{1290}{\kilo\gram\per\metre\cubed} \\
$r_b$      & Ballast radius                            & \qty{55}{\milli\metre} \\
$m_b$      & Net mass added per installed nut          & \qty{+1.87}{\gram} \\
$\Delta I_b$ & Net inertia added per installed nut     & $5.70\times10^{-6}$\,kg\,m$^2$ \\
$N$        & Nut count reaching $I_{w,\text{target}}$  & 15 \\
$m_w$      & Resulting wheel mass                      & \qty{156}{\gram} \\
$I_w$      & Resulting wheel inertia                   & $4.341\times10^{-4}$\,kg\,m$^2$ \\
--         & Achieved fraction of target               & \qty{103.3}{\percent} \\
\bottomrule
\end{tabular}
\end{table}

The bare ring and spokes supply $3.486\times10^{-4}$\,kg\,m$^2$, or
\qty{83}{\percent} of the target, so the ballast contributes the remaining
\qty{17}{\percent} --- which is the intended proportion: enough that the trim is
meaningful in both directions, not so much that the wheel depends on it.

The ballast is characterised \emph{net} of the plastic each pocket displaces:
$m_b$ and $\Delta I_b$ are the increments the installed bolt-and-nut adds over
the material it replaces, evaluated at the ballast radius. Sizing then reduces
to solving
\[
I_{w}(D_w, N) \;=\; I_{w,\text{ring}}(D_w) + N\,\Delta I_b \;\geq\; I_{w,\text{target}}
\]
for the smallest admissible $N$ at each candidate $D_w$, subject to the fit and
mass checks below.

\paragraph{Trade study.} Table~\ref{tab:rw_sizing} is the record sheet for the
outer-diameter sweep, populated once $I_{w,\text{target}}$ and the envelope cap
are known. Each candidate diameter is evaluated on four criteria, and the
selected design is the one that passes all of them with the widest trim margin:
\begin{enumerate}
  \item \textbf{Envelope.} $D_w$ must clear the motor subframe, the motor body
        and the two orthogonal wheel modules inside the selected $L$.
  \item \textbf{Inertia.} $I_w \geq I_{w,\text{target}}$, with margin for the
        uncertainty in $M$.
  \item \textbf{Pocket fit.} Enough plastic wall must survive between adjacent
        pockets and radially inboard and outboard of each. Small diameters fail
        here: they need so many bolts to reach the target that no wall remains
        between them.
  \item \textbf{Mass and trim margin.} The three-wheel set must stay within its
        allocation in Table~\ref{tab:massbudget}. Diameters large enough that
        the bare ring already exceeds the target are rejected---they leave no
        downward trim authority and cost mass for nothing.
\end{enumerate}
Criteria 3 and 4 bracket the feasible range from below and above respectively,
so the selection is the largest diameter that still requires a non-trivial bolt
count.

\begin{table}[h]
\centering
\caption{Reaction-wheel outer-diameter trade study against
$I_{w,\text{target}} = 4.202\times10^{-4}$\,kg\,m$^2$. $N$ is the nut count
reaching the target, rounded up to a multiple of the spoke count for balance;
the wall column is the plastic left circumferentially between adjacent pockets
against a \qty{2}{\milli\metre} minimum.}
\label{tab:rw_sizing}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{ccccccl}
\toprule
$D_w$ (mm) & $N$ & Mass (g) & $I_w$ ($10^{-4}$) & $I_w$/target (\%) & 3 wheels (g) & Circ.\ wall \\
\midrule
80  & 210 & 461 & 4.21 & 100.1 & 1382 & \textcolor{red}{Overlap} \\
90  & 141 & 344 & 4.21 & 100.2 & 1031 & \textcolor{red}{Overlap} \\
100 & 96  & 271 & 4.27 & 101.7 & 814  & \textcolor{red}{Overlap} \\
110 & 60  & 216 & 4.22 & 100.5 & 648  & \textcolor{red}{Overlap} \\
120 & 36  & 183 & 4.33 & 103.0 & 550  & \textcolor{red}{Overlap} \\
\textbf{130} & \textbf{15} & \textbf{156} & \textbf{4.34} & \textbf{103.3} & \textbf{468} & \textbf{\qty{11.0}{\milli\metre} OK} \\
140 & \multicolumn{6}{l}{Bare ring exceeds target ($4.51\times10^{-4}$) --- no downward trim} \\
150 & \multicolumn{6}{l}{Bare ring exceeds target ($5.73\times10^{-4}$) --- no downward trim} \\
\bottomrule
\end{tabular}
\end{table}

The trade brackets tightly. Below \qty{130}{\milli\metre} every candidate fails
criterion~3: reaching the target at a smaller radius needs so many nuts that the
pockets overlap and no plastic wall survives between them --- at
\qty{120}{\milli\metre}, 36 nuts on a \qty{50}{\milli\metre} ballast circle
leaves a negative wall. At \qty{140}{\milli\metre} and above the bare ring
already exceeds the target, failing criterion~4: the wheel costs mass for
nothing and, more importantly, offers no downward trim authority if the measured
inertia overshoots. \qty{130}{\milli\metre} is the only diameter in the sweep
that passes all four criteria, and it does so with \qty{11.0}{\milli\metre} of
circumferential wall --- comfortably clear of the minimum rather than marginal.

\paragraph{Trim authority.} The selected wheel sits at \qty{103.3}{\percent} of
target with 15 nuts, and the pocket-placement limit below caps the ring at 21.
The wheel can therefore be trimmed \emph{upward} by up to six nuts
($+3.4\times10^{-5}$\,kg\,m$^2$, about \qty{8}{\percent}) and downward by
removing them. This is the margin the mass-budget risk of
Section~\ref{sec:massbudget} draws on, and it is finite: it absorbs a moderate
overrun in $M$, not an open-ended one.

\paragraph{Radial and floor fit.} The radial checks pass in both nut
orientations, which removes an assembly error mode --- the nut fits either way
round. A \qty{12.01}{\milli\metre} pocket across corners in a
\qty{20}{\milli\metre} ring leaves \qty{4.00}{\milli\metre} of wall each side at
worst (\qty{4.80}{\milli\metre} across flats), against the
\qty{2}{\milli\metre} minimum. The pockets are blind rather than
through-holes: a \qty{5.2}{\milli\metre} pocket in the \qty{12}{\milli\metre}
ring leaves a \qty{6.8}{\milli\metre} plastic floor beneath each nut, and that
floor is a structural element rather than leftover material, since it is what
reacts the centrifugal load.

\paragraph{Pocket placement.} The pockets cannot be spaced uniformly, and this
is a hard geometric result rather than a preference. A spoke occupies a finite
angular width where it meets the ring, as does a pocket at the ballast radius,
so a pocket centre must clear a spoke centre by the sum of the two half-widths.
At the selected geometry the spoke half-angle at the ring inner diameter is
\ang{6.38} and the pocket half-angle at $r_b$ is \ang{6.27}, requiring
\ang{12.65} of separation. With $N = 15$ and three spokes the uniform pitch of
\ang{24} divides the spoke pitch exactly, placing every fifth pocket directly on
a spoke root; offsetting by half a pitch does not rescue it either. No uniform
pitch clears the spokes at any usable nut count.

The pockets are therefore distributed in equal groups within each inter-spoke
sector, spread over the \ang{94.7} of usable arc that remains once the spoke
exclusion zones are removed. Grouping by sector preserves the three-fold
symmetry and hence static and dynamic balance, and it caps the ring at seven
pockets per sector --- 21 in total, which is the source of the upward trim limit
quoted above. This constraint is invisible to the sizing calculation, which
models the ring as a bare annulus with no knowledge of where the spokes attach,
so it is carried into the CAD as an explicit requirement
(Appendix~\ref{app:cad_3d}) rather than left to be discovered when the wheel is
drawn.

\paragraph{Ballast retention.} Each nut carries a centrifugal load
$F = m_b\,\omega_{\max}^2\,r_b = \qty{54.3}{\newton}$ (\qty{5.5}{\kilo\gram} equivalent)
at the design speed, reacted by the \qty{6.8}{\milli\metre} pocket floor in
bending. The floor will carry it, but the floor alone is not a retention plan:
each nut is a loose steel mass in a plastic pocket on a wheel whose rim moves at
\qty{40.8}{\metre\per\second}, and the load scales with $\omega_{\max}^2$, so
any later decision to raise the design speed raises it quadratically.

Positive retention is therefore required and is provided by a retaining cover
plate over the pocket mouths. This is chosen over adhesive or threadlocker
specifically because it preserves the removability that motivates the entire
ballast scheme --- bonding the nuts in place would buy retention at the cost of
the bidirectional trim that the mass-budget risk of
Section~\ref{sec:massbudget} depends on. The retention method must be settled
before the wheels are printed, since it affects the ring geometry.

\paragraph{Balance as an assembly-control problem.} Fifteen discrete
\qty{2.5}{\gram} masses at \qty{55}{\milli\metre} radius make wheel balance a
matter of assembly verification, not just of design symmetry: a single missing
or misplaced nut is a \qty{2.5}{\gram} imbalance at that radius.
Section~\ref{sec:background} notes that a wheel half a gram out of balance at
rim radius can render a correct estimator unusable, so nut-count and position
verification per wheel is part of the balance and spin test rather than an
inspection afterthought.

\subsection{Electrical Design}
\label{sec:electrical}

The electrical architecture is presented here at the level of the four decisions
that shape it: what the components are, how power is distributed, how the buses
are routed and terminated, and how the whole is made safe to power on. The
sheet-level schematics, the connector and pin tables, the harness drawings and
the perfboard layout are collected in Appendix~\ref{app:electrical}, for the
same reason the CAD detail is separated from the mechanical rationale --- a
connector re-pin should not require an edit here.

\subsubsection{Components}

The electronics and software will be built around a microcontroller communicating over a CAN bus with a motor driver that closes the current loop for the selected brushless motor. Wheel speed will be measured with a magnetic encoder on the motor shaft, and body tilt will be estimated from an onboard IMU. Braking will be actuated by a digital servo driven directly from the microcontroller, and a wireless module will provide a telemetry and debugging link. Power will be supplied by a LiPo battery, stepped down through a DC-DC converter for the logic-level electronics, with the motor driver powered directly from the battery bus.

The full parts list is catalogued in Appendix~\ref{app:bom}
(Table~\ref{tab:bom-supplied}). The supplied set is an electronics-only BOM;
the reaction wheels, the frame and all printed parts are the team's design
responsibility, and their fabrication is the leading schedule risk.

\subsubsection{Power Architecture}
\label{sec:power_arch}

The power architecture is shown in Figure~\ref{fig:power_tree}. A single 6S LiPo
pack (\qty{22.2}{\volt} nominal, \qty{3600}{\milli\ampere\hour}) feeds everything
through one XT90 connector, behind a combined E-stop and fuse using an XT90-S
anti-spark connector to limit inrush into the driver bulk capacitance. The bus
then splits into two rails. The \emph{motor bus} runs at pack voltage
(\qtyrange{22}{25}{\volt} across the discharge curve) directly into the three
moteus drivers, with bulk capacitance rated to at least \qty{50}{\volt} to absorb
regenerative braking transients---which are not incidental here, since the
jump-up maneuver of Section~\ref{sec:jumpup} dumps the full wheel kinetic energy
back into the bus. The \emph{logic rail} is derived by an LM2596 buck converter
stepping \qty{22}{\volt} down to \qty{5}{\volt} for the Teensy, the ESP32
telemetry link, the brake servo and the IMU. Keeping the two rails separate
downstream of the E-stop means motor-current ripple does not reach the sensing
and processing electronics.

\begin{figure}[h]
\centering
\includegraphics[width=0.9\textwidth]{images/cubli_power_tree.png}
\caption{Power distribution tree. A single 6S pack feeds the motor bus directly
and the \qty{5}{\volt} logic rail through a buck converter, both behind a common
E-stop and fuse.}
\label{fig:power_tree}
\end{figure}

The rail-by-rail current budget that sizes the buck converter and the harness
conductors is Table~\ref{tab:power_budget}. It is populated from measurement
during bench bring-up rather than from datasheet maxima alone, because the
figure that matters for the \qty{5}{\volt} rail is the coincident worst case ---
telemetry transmit burst, servo stall and full sensor load at once --- and not
the sum of typical values.

\begin{table}[h]
\centering
\caption{Rail current budget. Measured values replace estimates during bench
bring-up; the \qty{5}{\volt} total is checked against the LM2596 rating, which
derates without a heatsink.}
\label{tab:power_budget}
\small
\begin{tabular}{L{4.4cm} c c c L{3.4cm}}
\toprule
Load & Rail & Typical (mA) & Worst case (mA) & Basis \\
\midrule
Teensy 4.1                & \qty{5}{\volt}  & \textcolor{red}{TBD} & \textcolor{red}{TBD} & Datasheet / measured \\
XIAO ESP32-C6 telemetry   & \qty{5}{\volt}  & \textcolor{red}{TBD} & 300 & TX burst governs \\
BMI270 IMU                & \qty{5}{\volt}  & \textcolor{red}{TBD} & \textcolor{red}{TBD} & Datasheet \\
MG92B brake servo         & \qty{5}{\volt}  & \textcolor{red}{TBD} & \textcolor{red}{TBD} & Stall current governs \\
MA600 encoders ($3\times$)& \qty{5}{\volt}  & \textcolor{red}{TBD} & \textcolor{red}{TBD} & Datasheet \\
\midrule
\textbf{Logic rail total} & \qty{5}{\volt}  & \textcolor{red}{\textbf{TBD}} & \textcolor{red}{\textbf{TBD}} & \textbf{vs.\ \qtyrange{2}{3}{\ampere} converter rating} \\
\midrule
moteus-n1 drivers ($3\times$) & Pack & \textcolor{red}{TBD} & \textcolor{red}{TBD} & Spin-up, not balance, governs \\
\bottomrule
\end{tabular}
\end{table}

The logic-rail total is the figure to close first. The converter is rated
\qtyrange{2}{3}{\ampere} and derates without a heatsink, while the coincident
worst case --- telemetry transmit burst at \qty{300}{\milli\ampere}, servo
stall, and the full sensor load simultaneously --- is not obviously inside that
rating. Summing it from measurement is a bring-up task
(Appendix~\ref{app:electrical_measured}), not a design assumption.

The bulk capacitance on the pack bus is sized by the regenerative transient
rather than by ripple: braking the wheels returns their stored energy
(\qty{86}{\joule} per wheel at the design speed, Section~\ref{sec:rw_sizing})
into the bus, so a \qtyrange{470}{1000}{\micro\farad} low-ESR part rated at
\qty{50}{\volt} or above is required. This is a distinct component from the
small electrolytics on the logic rail, which are low-voltage parts and must
never be fitted to the pack bus --- a confusion the isolation check of
Appendix~\ref{app:checklists} exists to catch.

\subsubsection{Schematic and Interconnect Diagrams}
\label{sec:schematics}

The signal-level design is documented as a set of sheets rather than a single
drawing, so that each can be checked against the constraint that governs it.
Table~\ref{tab:schematic_sheets} is the index; the sheets themselves are in
Appendix~\ref{app:schematics}, together with the connector and pin-assignment
tables that a person holding the harness needs and a reader of this section does
not.

\begin{table}[h]
\centering
\caption{Schematic sheet index. Each sheet is checked against the governing
constraint stated in its row before release.}
\label{tab:schematic_sheets}
\small
\begin{tabular}{c L{4.2cm} L{7.4cm}}
\toprule
Sheet & Content & Governing constraint to verify \\
\midrule
1 & Power entry and protection & E-stop, fuse rating, anti-spark inrush into the driver bulk capacitance \\
2 & \qty{5}{\volt} logic rail & Converter rating vs.\ Table~\ref{tab:power_budget} worst case; back-feed diode; rail bulk capacitance voltage rating \\
3 & CAN-FD bus & Linear topology, stub length, split termination at the two physical ends only (Sec.~\ref{sec:bus_topology}) \\
4 & Encoder interfaces ($3\times$) & SPI cable length and encoder air gap; driver placement follows from this, not the reverse \\
5 & Flight computer and IMU & Sensor orientation and mounting location relative to the pivot (Sec.~\ref{sec:control2d}) \\
6 & Telemetry link & Transmit-burst current and its return path off the logic rail \\
7 & Brake servo drive & Stall current headroom on the logic rail; PWM routing away from the encoder SPI \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Bus Topology, Termination and Signal Integrity}
\label{sec:bus_topology}

The CAN-FD bus is the one electrical subsystem whose physical routing is a
correctness requirement rather than a packaging preference, and it is therefore
fixed before the board outline is drawn. At the \qty{5}{\mega\bit\per\second}
design rate the bus must be a linear chain with stubs under
\qty{10}{\centi\metre} and split termination
($2\times\qty{60.4}{\ohm}$ with a \qty{4.7}{\nano\farad} capacitor to ground at
the midpoint) at exactly the two physical ends; a star topology or a mid-chain
termination will pass a bench continuity check and then fail intermittently
under wheel vibration, which is the hardest class of fault to diagnose once the
cube is closed. The design rules and their verification are listed in
Table~\ref{tab:signal_rules}.

The encoder interfaces impose the complementary constraint, and it is the more
restrictive of the two. Each MA600 requires an SPI run under
\qty{20}{\centi\metre} to its own driver and an air gap to its ring magnet held
below \qty{0.5}{\milli\metre}, with the sensor centred on the magnet thickness.
The driver positions are therefore dictated by the encoder positions, and those
in turn by the motor axes. The physical layout order is: motor axes fix the
encoders, encoders fix the drivers, drivers fix the bus route, and only then is
the board outline of Section~\ref{sec:layout} drawn around what remains. The
shim value that sets the air gap is established on the bench and becomes an
assembly specification carried into Appendix~\ref{app:cad_fab}, since the gap
must be re-established every time the structure is bolted up.

\begin{table}[h]
\centering
\caption{Signal-integrity design rules and how each is verified. These are
checked before cube closure, since all are hard to access afterwards.}
\label{tab:signal_rules}
\small
\begin{tabular}{L{3.4cm} L{4.6cm} L{6.0cm}}
\toprule
Interface & Rule & Verification \\
\midrule
CAN-FD bus       & Linear chain; stubs $<\qty{10}{\centi\metre}$; split termination ($2\times\qty{60.4}{\ohm}$, \qty{4.7}{\nano\farad}) at the two physical ends only & Differential-pair scope capture; sustained error-frame count at \qty{5}{\mega\bit\per\second} with all nodes addressed \\
Encoder SPI      & Run $<\qty{20}{\centi\metre}$ to its own driver; air gap $\le\qty{0.5}{\milli\metre}$, sensor centred on the magnet & Read-back with no error flags; re-checked after mechanical assembly \\
IMU              & Mounted at or near the pivot; orientation per the body frame of Sec.~\ref{sec:cad_method} & Static orientation check; noise floor with wheels spinning \\
Rail separation  & Motor bus and logic rail separated downstream of the E-stop & Ripple measurement on the logic rail under motor load \\
Grounding        & Single return topology; no motor return current through the sensing ground & Continuity and isolation check before first power-on \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Board Layout and Harness}
\label{sec:layout}

The electronics are built on a \qtyproduct{150 x 90}{\milli\metre} double-sided
perfboard rather than a fabricated PCB. This is a schedule decision, not a
technical preference: fabrication turnaround does not fit inside the build
window, and a perfboard can be modified in place when bring-up reveals a change.
Its consequence is that layout discipline --- rail separation, decoupling at
each device, deliberate routing of the bus pair --- must be enforced by hand and
checked explicitly, since no design-rule check will do it.

The board dimension is also a driver of the cube envelope rather than a
consequence of it: at \qty{150}{\milli\metre} across, the board does not fit
inside a \qty{150}{\milli\metre} cube at any wall thickness, which is the
packaging argument that independently forces the \qty{180}{\milli\metre}
envelope of Section~\ref{sec:envelope_selection}. If the board can be split into
two smaller boards that argument weakens considerably and the envelope decision
rests more heavily on wheel trim authority; that question is settled during
floorplanning and before the outline is delivered.

The board outline, mounting-hole pattern, keep-out heights and connector exit
directions constitute the electronics-mount interface of
Table~\ref{tab:mech_interfaces} and are frozen once delivered to the mechanical
design: later electrical changes must fit inside that outline rather than move
it. The populated board mass is a required input to the mass budget of
Table~\ref{tab:massbudget} and is obtained by weighing the assembled prototype,
not estimated. The harness --- battery lead, driver drops, bus chain and servo
drive --- is fabricated to the drawings in Appendix~\ref{app:harness} and is
labelled and strain-relieved before installation, because every connector in the
machine sits on a structure that vibrates by design.

\subsubsection{Electrical Safety and Bring-Up Protocol}
\label{sec:electrical_safety}

Two rules govern all electrical bring-up and are stated here because they are
design constraints, not merely lab practice. First, every first-time power
application is made on a current-limited bench supply, never on the battery; the
battery is introduced only after the assembly has passed a full bench power-on.
Second, the pack is never connected without the E-stop and fuse in circuit and
the anti-spark path intact, since the driver bulk capacitance draws an inrush
that will weld a bare connector.

The staged sequence --- rails, then buses, then sensors, then open-loop motion,
then closed-loop --- is part of the validation plan and is given in
Section~\ref{sec:testing}. The isolation checks that precede first power-on
(rail-to-rail separation, bus-to-chassis, and confirmation that no low-voltage
capacitor sits on the pack bus) are recorded in Appendix~\ref{app:electrical}
as a checklist, because they are performed on every re-assembly and not only
once.

\subsection{System Integration}
\label{sec:integration}

% Mechanical mounting and interface management: TBD pending CAD.

\subsubsection{Onboard Software Pipeline}
\label{sec:software_pipeline}

The subsystems of Figure~\ref{fig:system_loop} are coordinated by a single
deterministic task on the Teensy, whose structure is given in
Figure~\ref{fig:software_pipeline}. Each \qty{1}{\milli\second} cycle executes
the same fixed sequence: sensor ingest reads the BMI270 over SPI and the moteus
replies over CAN; the MEKF propagates attitude on the gyro and corrects it
against the accelerometer while estimating gyro bias; the state vector
$x = [g_B, \omega, h]$ is assembled from the estimator output and the wheel
speeds; the LQR law of Section~\ref{sec:control2d} evaluates $u = -Kx$ with a
penalty on stored momentum $h$ to bleed off wheel saturation; and the resulting
commands leave as CAN torque requests to the drivers and a PWM signal to the
brake servo. Telemetry is tapped off the assembled state to SD and Wi-Fi outside
the control path, so logging cannot perturb loop timing.

The fixed ordering matters: with no dynamic allocation and no blocking calls, the
worst-case cycle time is bounded by construction rather than measured after the
fact, which is what makes the \qty{1}{\kilo\hertz} sample period of the discrete
LQR design a design guarantee rather than an average.

\begin{figure}[h]
\centering
\includegraphics[width=0.78\textwidth]{images/teensy_software_pipeline.png}
\caption{Teensy 4.1 control-loop software pipeline. The estimator and controller
stages implement the design of Section~\ref{sec:control2d}; telemetry is a
side-branch off the control path.}
\label{fig:software_pipeline}
\end{figure}

\subsubsection{Control Software Architecture}
\label{sec:control_sw}

Section~\ref{sec:modeling} derives the control law; this subsection states how
that law is realised as software, which is a separate set of decisions. The
implementation detail --- the mode state machine, the estimator and controller
in executable pseudocode, the fixed gains and tuning parameters, the timing
budget and the fault responses --- is collected in
Appendix~\ref{app:control_algorithms}, so that a re-tuned gain does not require
an edit to the design sections.

The software is organised in the four layers of
Table~\ref{tab:sw_layers}. The division is not stylistic: each layer has a
different failure mode and a different verification method, and the interfaces
between them are what allow the controller validated in simulation to be the
same code that runs on the cube.

\begin{table}[h]
\centering
\caption{Control software layers. Each is verified by a different method, which
is why the boundaries are held.}
\label{tab:sw_layers}
\small
\begin{tabular}{L{2.8cm} L{5.4cm} L{5.6cm}}
\toprule
Layer & Responsibility & Verified by \\
\midrule
Hardware abstraction & Sensor and driver I/O, timing, bus transactions & Bench read-back; loop-timing measurement \\
Estimation           & Attitude and rate estimate from IMU and encoders (Sec.~\ref{sec:control2d}) & Static and dynamic bench tests against a known reference \\
Control              & State feedback, momentum management, saturation handling & Simulation on the identified model, then hardware \\
Supervision          & Mode transitions, arming, watchdog and fault response & Fault-injection tests on the rig \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Operating modes.} The machine is never in a single control regime.
The supervisory layer sequences the modes of Table~\ref{tab:sw_modes}, with
hysteresis on every threshold to prevent chattering at a boundary, and with a
disarm transition available from every mode. Documenting the modes as a state
machine rather than as a set of conditionals matters here because the dangerous
transitions are the ones back down the ladder: a fall detected during corner
balance must reach a safe state with the wheels commanded down, not merely
switch to a different gain.

\begin{table}[h]
\centering
\caption{Operating modes and transitions. Every mode has a disarm exit; the
detailed conditions and hysteresis bands are in
Appendix~\ref{app:control_algorithms}.}
\label{tab:sw_modes}
\small
\begin{tabular}{L{2.6cm} L{5.2cm} L{6.0cm}}
\toprule
Mode & Behaviour & Exit condition \\
\midrule
Idle / disarmed & Drivers de-energised; sensors and telemetry live & Explicit arm command \\
Wheel spin-up   & Open-loop speed ramp to the jump-up momentum target & Target wheel speed reached \\
Jump-up         & Brake actuation and momentum transfer (Sec.~\ref{sec:jumpup}) & Tilt inside the balancing capture band \\
Edge balance    & Single-axis state feedback (Sec.~\ref{sec:control2d}) & Corner-transition command, or fall detected \\
Corner balance  & Three-axis state feedback (Sec.~\ref{sec:3dmodel}) & Slew command, or fall detected \\
Slew / rotation & Reference tracking between equilibria & Reference reached; revert to balance \\
Fault / safe    & Wheels commanded down, drivers disarmed & Manual reset \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Real-time guarantees.} The loop-rate claim of
Section~\ref{sec:control2d} is a design guarantee only if the worst-case cycle
is bounded by construction, which is why the pipeline of
Figure~\ref{fig:software_pipeline} uses a fixed stage ordering with no dynamic
allocation and no blocking calls, and why telemetry is a side-branch rather than
a stage. The per-stage timing budget and its measured margin are recorded in
Appendix~\ref{app:control_algorithms} and re-measured whenever a stage changes.

\paragraph{Fault handling.} The failure cases the software must survive are
sensor dropout, bus error, wheel saturation and loss of the balancing
equilibrium. The first three have defined responses that keep the machine
controlled; the fourth is a controlled shutdown rather than a recovery attempt,
since a cube that has left its capture region cannot be recovered by the
linear law and continued actuation only adds energy to the fall.
